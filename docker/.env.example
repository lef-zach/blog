# ============================================
# DOCKER ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env and customize for your environment
# DO NOT commit .env to git as it contains sensitive information

# ============================================
# CORS ORIGIN CONFIGURATION
# ============================================
# The frontend URL that is allowed to make API requests to the backend
# This is critical for CORS (Cross-Origin Resource Sharing) to work
#
# Examples:
# - Local development: CORS_ORIGIN=http://localhost:3000
# - VPS with IP: CORS_ORIGIN=http://192.168.0.136:3000
# - Production domain: CORS_ORIGIN=https://yourdomain.com
# - Multiple origins (comma-separated): CORS_ORIGIN=http://localhost:3000,https://yourdomain.com
#
# IMPORTANT: When deploying to a VPS, browsers resolve "localhost" to the client machine,
# not the VPS. You MUST use the actual VPS IP address or domain name.
#
# For the current setup (VPS at 192.168.0.136):
CORS_ORIGIN=http://192.168.0.136:3000

# ============================================
# API URL CONFIGURATION
# ============================================
# The backend API URL that the frontend will use to make requests
# This is used by the Next.js frontend to construct API URLs
#
# Examples:
# - Local development: NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
# - VPS with IP: NEXT_PUBLIC_API_URL=http://192.168.0.136:3001/api/v1
# - Production domain: NEXT_PUBLIC_API_URL=https://yourdomain.com/api/v1
#
# For the current setup (VPS at 192.168.0.136):
NEXT_PUBLIC_API_URL=http://192.168.0.136:3001/api/v1
